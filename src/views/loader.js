const Loader = {
    view: () => {
        return [
            m("div.content.content-fixed.content-auth",
                m("div.container",

                )
            )
        ];
    },
};

export default Loader;